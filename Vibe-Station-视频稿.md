# Vibe Station 项目讲解视频稿

> 适用场景：录屏 + 口播，讲解一个个人 AI 开发工作台全栈项目
> 预计总时长：8 ~ 10 分钟
> 技术栈：Nuxt 4.5（Vue 3.5 + Tailwind v4）+ Nitro + SQLite，pnpm monorepo

---

## 0. 开场（0:00 – 0:40）

【屏幕】项目首页 `http://localhost:3000/`（或已部署地址），鼠标在页面上轻轻移动

**口播：**
大家好，今天给大家讲一个我自己做了大概三周的个人项目——**Vibe Station**。
它不是什么 To B 的 SaaS，就是我个人用的一个 **AI 开发工作台**：把「云端大模型」和「端侧本地大模型」两种能力，塞进同一个界面里。
你可以把它理解成：一个能一键在「联网的 DeepSeek」和「完全跑在你浏览器里、数据不出本机的本地模型」之间切换的 AI 工作台。
接下来我会从架构、核心亮点，再到我踩过的几个坑，带大家过一遍。

---

## 1. 项目定位与技术栈（0:40 – 2:00）

【屏幕】切到编辑器，展示仓库根目录结构（`apps/`、`packages/`），再展示 `package.json` 里的 dependencies

**口播：**
先说技术选型。整个项目是一个 **pnpm monorepo**，分两大部分：
- `apps/web` 是前端 + 服务端，跑在 **Nuxt 4.5** 上——也就是 Vue 3.5 加 Tailwind v4；
- `packages/` 下面有三个独立包：`ai-core`（AI 能力抽象）、`db`（数据库 schema）、`shared`（公共类型）。

为什么用 Nuxt？因为它自带的 **Nitro** 服务端框架，让我不用再单独起一个后端。前端页面、API 路由、流式转发，全在一个仓库里搞定。
数据库用的是 **SQLite**，配 WAL 模式，部署简单，一个文件就能跑。
这就是典型的「个人项目最优解」：能少一个服务就少一个服务。

---

## 2. 四层架构总览（2:00 – 3:30）

【屏幕】展示之前画的架构图（或切到 README 的架构说明），逐层高亮

**口播：**
整个应用的架构可以分成四层，从外到内：

**第一层，UI 层。** 一共六个模块：首页、AI Chat、Prompt Studio、Code Vault、博客/开发日志、还有 Showcase。其中 AI Chat 是核心，支持「消息级」切换模型。

**第二层，BFF 层（就是 Nitro 服务端）。** 这一层很关键，它做三件事：
1. 所有 API 走 Access Token 中间件做鉴权；
2. 把云端大模型的**流式响应（SSE）**中转给前端；
3. 做 IP 限流，每分钟最多 10 次云端调用。
更重要的是——**API Key 只存在服务端环境变量里，前端永远拿不到**。这是个人项目最容易翻车的地方，我一开始就堵上了。

**第三层，AI Core。** 这里把两种模型抽象成统一接口：云端是 DeepSeek（OpenAI 兼容协议），本地是 WebLLM 跑的 Qwen 模型。

**第四层，数据层。** Drizzle + SQLite，六张表：博客文章、Prompt、Prompt 运行记录、代码片段、聊天会话、聊天消息。

---

## 3. 核心亮点：双模型一键切换（3:30 – 5:30）

【屏幕】打开 `/chat` 页面，演示在对话中切换「云端 / 本地」模型；展示 `useLocalLLM.ts` 里的 `MODEL_ID` 常量

**口播：**
这个项目的灵魂，就是**双模型架构**。
你看聊天界面，每一条消息都能选：是用云端模型，还是本地模型。

先说**云端模型**——DeepSeek。它需要联网和 API Key，优势是能力强、回答质量高。
再说**本地模型**——我现在配的是 **Qwen2-0.5B-Instruct**，一个 5 亿参数的小模型，但它**完全跑在浏览器里的 WebGPU 上**，权重通过 `/api/llm-model/...` 这个代理下载，**数据不出本机**。

为什么两个都要？因为场景不一样：
- 写代码、复杂推理 → 用云端；
- 只是本地随便聊、或者不想把敏感内容发出去 → 切本地。
一键切换，同一个聊天框，体验无缝。

【屏幕】演示点击「本地模型」触发下载，进度条走动

**口播：**
本地模型第一次用需要下载权重，大概几百兆。下载完之后 WebLLM 会把模型缓存到浏览器的 IndexedDB，之后打开就能直接用，不用重复下载。

---

## 4. 踩坑故事：本地模型链路的三个 Bug（5:30 – 7:00）

【屏幕】展示 `useLocalLLM.ts` 顶部注释，和 `server/utils/llm-model.ts` 的代理逻辑

**口播：**
讲真，本地模型这块坑特别多，我前后修了三个比较典型的 bug，正好跟大家分享一下，免得你们踩。

**第一个，Tokenizer 单例报错。**
报错信息是 `Expected null or instance of Tokenizer, got an instance of Tokenizer`。
原因很有意思：WebLLM 底层是个 WASM 运行时，如果你把它多次加载，就会注册出**两个独立的运行时**，前一个运行时创建的 Tokenizer 在另一个里校验就失败。
在 Vite 的热更新（HMR）下特别容易触发。
解决办法参考了 GitHub 上的 issue #732：把 engine 实例用模块顶层的普通变量存着，**绝对不能放进 Vue 的 `ref` 里**——否则会被 Vue 的响应式系统代理，又搞出第二个运行时。我还额外把它挂到 `window` 上做全局单例，并关掉这个模块的 HMR。

**第二个，模型分片下载 500。**
修好 Tokenizer 之后，模型权重下载又报 `Failed to execute 'add' on 'Cache'`。
追到底，是代理路由里我对上游 `fetch()` 没做异常捕获，网络一抖（hf-mirror 会 308 重定向到 huggingface，国内网络不稳）就冒泡成 500。
改成捕获网络异常、返回可重试的 502，前端识别到就自动重试一次，问题就消了。

**第三个，Qwen 代理 404。**
换成 Qwen 模型之后，`mlc-chat-config.json` 一直 404。
一查，代理路由里把模型 ID **硬编码成了 Phi-3**，只放行那一个模型的路径。
改成用正则匹配任意 `{modelId}/resolve/main/{file}` 路径，代理就支持所有 WebLLM 模型了。

这三个 bug 修完，本地 Qwen 模型终于能稳定下载、稳定对话了。

---

## 5. 工程化与部署（7:00 – 8:00）

【屏幕】展示 `scripts/Dockerfile`、`deploy.sh`、`backup-db.sh`，以及 `vitest` 测试运行结果

**口播：**
最后说一下工程化。个人项目也不能太「野生」。

- **一键部署**：写了 Dockerfile 加部署脚本，数据卷挂载 `./data`，一条命令就能起；
- **数据库自动备份**：`backup-db.sh` 配 cron，每天凌晨 3 点自动备份 SQLite；
- **测试**：核心逻辑——SSE 解析、Prompt 变量渲染、还有刚才说的代理路由——都用 Vitest 覆盖了，目前 50 个测试用例全过。

这些看起来不起眼，但能让你半夜不怕服务器挂、改代码不怕改崩。

---

## 6. 结尾（8:00 – 8:30）

【屏幕】回到首页，定格在 GitHub 仓库或项目地址

**口播：**
好，这就是 Vibe Station——一个把云端和端侧 AI 揉在一起的个人工作台。
它不大，但该有的工程化都有；坑踩过，但也都填上了。
如果你也在做类似的全栈 AI 项目，希望这套双模型架构和那些踩坑经验能帮到你。
我是 [你的名字]，我们下期见。

---

## 附：录制小贴士

- **节奏**：第 4 段「踩坑故事」是视频最出彩的部分，建议稍微放慢、带点情绪，观众最爱看「怎么 debug」；
- **演示准备**：录之前先 `pnpm dev` 起服务，本地 Qwen 模型提前下好缓存，避免录制时卡在下载；
- **B-roll**：架构图、终端跑测试、浏览器里切换模型的画面，可以剪成穿插镜头；
- **字幕**：技术名词（WebLLM / WebGPU / SSE / HMR / IndexedDB）建议加字幕或画面标注，观众更容易跟上。
